run:
  type_data: test
  type_class: base
  split_type: base
  split_config:
    params:
      test_size: 0.2
      random_state: 42
      stratify: true

  type_class_model: transformers

full_data:
  data:
    path: C:\Users\Danch\PycharmProjects\Credit_Scoring_Model_w_transformers\data\Dataset_C\train_full.csv
    sheet_name: "sheet_name"
    target: "default_flg"
    dropna: False
    drop_columns:
      - "id"

  features:
    numeric:
    categorical:

model:
  model_name: tabtransformer_stage_D_Application
  framework: sklearn
  ensemble:
    type: transformers_transfer
    voting: soft
    final_estimator: null
    estimators:
      - name: tabtransformer_lending_projadapt
        type: Transformers
        params:
          dataset_name: "Application"
          embed_dim: 64
          n_heads: 8
          n_layers: 4
          mlp_dim: 256


          # ── Регуляризация ────────────────────────────────
          dropout: 0.2
          weight_decay: 0.1

          # ── Обучение ─────────────────────────────────────
          lr: 0.001
          batch_size: 128
          epochs: 35
          early_stopping_patience: 5
          warmup_ratio: 0.05
          lambda_align: 5
          adapt_epochs: 5

          class_weight: 'balanced'

          transfer_mode: 'proj_adapt'
          backbone_path: C:\Users\Danch\PycharmProjects\Credit_Scoring_Model_w_transformers\models\transformers_\tabtransformer_stageA_hc.pth
          freeze_mode: 'full'

metrics:
  base:
    - auc
    - ks
    - fnr
    - tpr
    - fpr
    - precision
    - accuracy
